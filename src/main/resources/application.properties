# Server configserver.port=${CLUSTER_SERVICE_PORT:9001}server.connection-timeout=30server.servlet.context-path=/api# tomcat configserver.tomcat.uri-encoding=UTF-8# application configspring.application.name=${CLUSTER_APP_NAME:po-mongo}